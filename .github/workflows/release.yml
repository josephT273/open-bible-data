name: Build and Release Open Bible Data

on:
  push:
    branches:
      - prod

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install
        
      - name: Generate SQLite Databases
        run: bun run bible.ts

      - name: Generate Manifest
        run: bun run manifest.ts

      - name: Update latest tag
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git tag -f latest
          git push origin -f latest

      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v1
        with:
          tag_name: latest
          name: "Latest Bible Data Update"
          body: "Automated database update. Includes latest SQLite files and JSON manifest."
          draft: false
          prerelease: false
          files: |
            sqlite/*.sqlite
            manifest.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
